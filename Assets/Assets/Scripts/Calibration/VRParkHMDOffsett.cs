using UnityEngine;


    public class VRParkHMDOffsett : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
           /* Vector3 savedPosition;
            savedPosition.x = PlayerPrefs.GetFloat(CalibrateRoom.POS_OFFSETT_KEY[0]);
            savedPosition.y = PlayerPrefs.GetFloat(CalibrateRoom.POS_OFFSETT_KEY[1]);
            savedPosition.z = PlayerPrefs.GetFloat(CalibrateRoom.POS_OFFSETT_KEY[2]);

            float angle = PlayerPrefs.GetFloat(CalibrateRoom.ANGLE_OFFSETT_KEY);

            transform.position = savedPosition;
            transform.localEulerAngles = new Vector3(0, angle, 0);*/
        }

    }

